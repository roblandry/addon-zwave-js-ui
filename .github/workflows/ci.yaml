---
name: CI

# yamllint disable-line rule:truthy
on:
  push:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
  workflow_dispatch:

jobs:
  workflows:
    uses: addon-zwave-js-us/.github/workflows/addon-ci.yaml@main
    with:
      slug: "zwave-js-ui"
